version: '3'
services:
   web:
     image: nginx
     ports:
       - "80"
     volumes:
       - /home/docker/:/var/www/html
   app:
     image: tomcat
     ports:
       - "8080"
   db:
     image: redis 
     ports:
        - "9080"
